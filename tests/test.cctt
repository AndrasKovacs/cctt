
foo := coe 0 0 (_. U → U) (λ x. x) U;

bar := hcom 0 0 (U → U) [] (λ x. x) U;

data Bot :=;
kek (x : Bot) := case x (_. U → U) [] U;

-- data Unit := tt;
-- idunit (x : Unit) : Unit := case x (_. Unit) [tt. tt];
-- test : idunit ={_. Unit → Unit} idunit := refl;
