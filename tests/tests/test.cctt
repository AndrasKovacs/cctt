
-- foo (A A : U) (x : @0) : @0 := ?;

data Unit := tt;
foo : Unit → Unit :=
  λ [tt. tt];

kek (Unit : U) := @@0;

lel (tt : U) := @@0#0;

id (A : U)(x : A) := x;

fun (A B : id U U) (x : A) (y : A) : A := ?;

fun2 (A B : id U U) (x : A) (y : A) : A := ?;

fun4 (A B : id U U) (x : A) (y : A) : A := ?mallac;