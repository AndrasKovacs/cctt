
-- higher inductive sTrunc (A : U) :=
--     sinc (a : A)
--   | strunc (a b : sTrunc A) (p q : a = b) (i j : I)
--       [i=0. p j
--       ;i=1. q j
--       ;j=0. a
--       ;j=1. b];

-- strunc' (A : U) (a b : sTrunc A) (p q : a = b) : p = p := λ i j. strunc a b p q i j;