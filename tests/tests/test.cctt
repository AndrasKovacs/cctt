
-- (BUG: with field proj doesn't check Omega)
ptType : U := (A : U) * (pt : A);
Omega (A : ptType) : ptType := (A.pt = A.pt, refl);

foo (A : ptType) (h : A.1 -> U) := U;

inductive Bool := true | false;

mallacc (i : I) : Bool → Bool := hcom 0 1 [i=0 _. λ x. x] (λ x. x);